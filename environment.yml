name: agentMet4FoF
dependencies:
    - python >= 3
    - ipykernel
    - numpy
    - scikit-learn
    - scipy
    - matplotlib
    - pandas
    - seaborn
    # pip dependencies have to be provided in `requirements.txt` syntax
    - pip:
        - osbrain
        - scikit-multiflow
